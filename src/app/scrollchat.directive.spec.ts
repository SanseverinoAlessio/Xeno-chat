import { ScrollchatDirective } from './scrollchat.directive';

describe('ScrollchatDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollchatDirective();
    expect(directive).toBeTruthy();
  });
});
